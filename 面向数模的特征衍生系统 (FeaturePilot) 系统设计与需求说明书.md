# 语义感知型特征工程流水线（Semantic-Aware Feature Engineering Pipeline）

> **核心定位**：一个**轻量级、面向竞赛的"语义感知型特征工程流水线"**——不是臃肿的黑盒 AutoML，而是充当编程手身边的**"高阶特征副驾驶"**。

---

## 1. 核心痛点：它到底要解决什么？

在数学建模竞赛或数据分析前期，数据清洗与特征构建往往占用大量时间，主要面临三个现实障碍：

### 1.1 暴力衍生的"特征爆炸"与无意义
传统工具（如 Featuretools）采用穷举相乘相除，20 个特征能瞬间衍生出上千个特征，不仅拖慢模型训练，而且在数模论文中无法解释其现实物理意义。

### 1.2 通用 EDA 工具"只看病不给药"
类似 `ydata-profiling` 只能输出相关系数热力图或直方图，告诉你"这里有偏态"、"那里有相关性"，但不会结合赛题背景告诉你该如何衍生新变量。

### 1.3 手写脚本耗时且易踩坑
在短时间内手写交叉特征，极易遇到**除以零、对负数取对数、时间戳未循环编码、数据泄露（Data Leakage）**等代码错误。

### ✅ 核心价值
把"**纯统计分析**"和"**业务背景理解**"结合起来，只输出**有数理机理解释的 Top 衍生特征**，并一键生成**绝不报错的原生 Pandas 代码**。**机器（统计）给候选、模型（语义）给机理、人（确认点）给最终拍板权**。

---

## 2. 整体设计思路："事实与语义分离"

> 很多初学者做 AI 数据工具，会盲目将整张数据表甚至几千行数据直接塞给大模型 → Token 爆炸、时延极长且极度不可控。

**系统设计核心：本地跑事实，云端跑语义，本地安全沙箱收口，关键决策留给人。**

### 2.1 本地引擎负责"统计事实"
Pandas/NumPy 在本地计算各列的元数据（列名、数据类型、偏度、缺失率、两两相关系数 Top 5、互信息），并完成**目标列探测与风险扫描**。**全量原始数据绝不上云**，既快又安全。

### 2.2 大模型负责"常识与机理推理"
大模型只接收元数据画像和用户写的一句话赛题背景（例如"预测工业锅炉能耗"）。它发挥自身常识储备，推演哪些变量具有物理关联（例如识别出"压力"和"容积"，推荐衍生"做功量"）。

### 2.3 本地沙箱负责"校验、映射执行与代码拼装"
大模型只输出**标准 JSON 格式的推荐意向**——而且从设计上收敛为**"受控算子名 + 参数列表"**，而不是自由公式字符串。本地通过 `SafeOperatorRegistry` 白名单将算子映射到原生安全函数执行与拼装，全程**禁用裸 `exec()`**，确保导出的代码 **100% 能够直接运行**。

### 2.4 人工确认点（Human-in-the-Loop 收口）
涉及**数据泄露风险（镜像列）**或**临界缺失率**的高危特征，系统永不静默放行：前端用红/黄卡片警示并**默认锁定勾选框**，必须由用户显式点击"我已了解风险并确认衍生"后才可进入代码导出。

---

## 3. 核心数据流动流程（Pipeline）

整个系统自上而下分为 5 个处理阶段，**阶段 1 与阶段 2 之间存在有/无监督自适应分支**：

```
[用户上传 CSV + 一句话赛题背景 + Target 选择(回归列/分类列/无特定目标)]
           ↓
[阶段 1：本地数据体检引擎 profiler.py]
    · 描述统计：mean/std/skewness/unique_ratio/缺失率/零值比率
    · Target 探测：有监督 → Pearson + Mutual Information（对 Target）；
                  无监督 → 方差/离散度/PCA 载荷/偏度（不依赖 Label）
    · 风险扫描：镜像列(r ≥ 0.999) → 高危黑名单；缺失率 40%~70% → 临界黄名单
           ↓
[阶段 2：Prompt 上下文打包]
    · 仅打包：元数据 Schema + 异常统计指标 + 风险黑/黄名单 + 建模目标(有/无监督)
           ↓
[阶段 3：LLM 结构化推理 recommender.py]
    · 强制 JSON：只返回 {feature_name, op, params, reason}，op 必须是受控算子
    · 离线/弱网降级：本地统计规则引擎
           ↓
[阶段 4：本地校验、安全执行与代码拼装 codegen.py]
    · 幻觉过滤：op ∈ SafeOperatorRegistry 且 params ⊆ 真实 df.columns
    · 沙箱映射执行：白名单原生函数算出新特征（本地预览绘图用，永不 exec）
    · 代码导出：拼接带防除零/防负数注释的原生 Pandas 代码字符串
           ↓
[阶段 5：Streamlit 看板 + 人工确认 app.py]
    · 绿/黄/红卡片展示；变换前后直方图/KDE 预览
    · 高危特征默认禁用勾选 → 用户点"我已了解风险并确认衍生" → 解锁
    · 一键复制 / 下载 .py 脚本
```

> **自适应分支决策**：阶段 1 结束即确定本次会话是"有监督"还是"无监督"模式，并携带该模式贯穿阶段 2/3（Prompt 目标不同）与阶段 4/5（推荐产出物不同）。同一套 5 阶段骨架，两种推理脑，无需单独建第二条流水线。

---

## 4. 系统架构模块划分（开发指南）

项目无需复杂的微服务，推荐采用**轻量模块化设计**：

```
project/
├── app.py                    # Streamlit 交互层
└── core/
    ├── profiler.py           # 数据分析、体检与风险扫描
    ├── recommender.py        # LLM 推荐 + 离线降级规则引擎
    └── codegen.py            # 受控算子沙箱、校验与代码拼装
```

### 4.1 `core/profiler.py` —— 数据分析、体检与风险扫描模块

**（1）常规画像**：加载 CSV，提取每列描述性统计量（`mean`、`std`、`skewness`、`unique_ratio`、缺失率、零值比率）；识别特殊列：**时间戳列、高基数类别列、常数列**。

**（2）Target 定义与有/无监督自适应打分**：

```python
def profile(df, target=None, task=None):
    """task: "regression" | "classification" | None(无监督)"""
    ...
    if target is not None:                       # ← 有监督
        scores = {
            "pearson": df[numerics].corrwith(df[target]),          # 线性相关
            "mutual_info": mutual_info_score(df, target),           # 非线性相关
        }
    else:                                        # ← 无监督降级
        scores = {
            "variance": df[numerics].var(),                        # 信息量
            "skewness": df[numerics].skew(),                       # 偏度
            "pca_load": abs(loading_on_first_2_pcs(df)),           # 主成分载荷
        }
    risks = detect_risks(df)   # 镜像列 / 高缺失率标记
    return Profile(meta=..., scores=scores, risks=risks, target=target)
```

**（3）风险扫描（Human-in-the-Loop 的输入端）**：
- **疑似数据泄露/镜像列**：两列相关系数 $r \ge 0.999$ → 标记为**高危红名单**，列入给 Prompt 的"禁用清单"，禁止大模型或本地规则自动衍生其交叉特征；
- **临界高缺失率**：缺失率落在 $40\% \sim 70\%$ → 标记为**临界黄名单**，禁止静默填充、禁止作为除法/交叉的活跃分母。

### 4.2 `core/recommender.py` —— 推荐与降级模块

- **主路径（有监督）**：向大模型 API 发起请求，上下文 = 精简 Schema + 异常指标 + **建模目标（"预测 `{target}`，任务类型 `{task}`"）** + 风险黑/黄名单 + 一句话赛题背景。利用 **Tool Calling 或 JSON Schema** 强制返回**受控算子 JSON**（非自由代码）。
- **主路径（无监督）**：不改代码结构，仅替换 Prompt 指令为"**无监督模式**：请在保留信息的前提下，推荐低冗余、可解释的衍生特征（优先启发高方差、高离散、强 PCA 载荷的列）"，并追加"禁止引用黑名单列"。
- **降级路径（离线 / 弱网）**：纯本地统计规则引擎。有监督时基于对 Target 的互信息 Top-k 挑列；无监督时基于方差/偏度/PCA 载荷挑列，并自动触发规则（高偏态 → `log1p`；镜像列 → 只保留其一；高危列 → 一律跳过）。

### 4.3 `core/codegen.py` —— 受控算子沙箱、校验与代码拼装模块

**（1）受控算子库 `SafeOperatorRegistry`（禁止裸 `exec()`）**：

| 算子名 `op` | 映射的原生安全函数 | 说明 | 导出的防御性 Pandas 代码要点 |
| --- | --- | --- | --- |
| `safe_divide` | 安全除法 | 除零保护 | `np.where(df['B']==0, np.nan, df['A']/df['B'])` |
| `log1p` | 对数变换 | 平移 + `log1p` 防 `log(0)/log(负)` | 先减列最小值移至非负，再 `np.log1p` |
| `diff` | 差分 | 时序一阶差分，首行 `NaN` 说明 | `df[X].diff()` + 缺失注释 |
| `sin_cos` | 时间循环编码 | 周期性特征高分技巧 | `sin(2π·h/period)`、`cos(2π·h/period)` |
| `interaction` | 多项式交互 | 交叉相乘 | `df[A] * df[B]`（仅数值列） |

**（2）安全执行模型**：本地**只查表、永不执行任意字符串**——

```python
REGISTRY = {
    "safe_divide": Operator(fn=_safe_divide,   export=_export_divide),
    "log1p":       Operator(fn=_safe_log1p,    export=_export_log1p),
    "diff":        Operator(fn=_safe_diff,     export=_export_diff),
    "sin_cos":     Operator(fn=_safe_sin_cos,  export=_export_sin_cos),
    "interaction": Operator(fn=_safe_mul,      export=_export_mul),
}

def apply(df, rec: dict):
    op    = rec["op"]
    params= rec["params"]
    assert op in REGISTRY,            "未注册算子 → 丢弃"
    assert set(cols(params)) <= set(df.columns), "幻觉列名 → 丢弃"
    return REGISTRY[op].fn(df, params)     # 白名单原生函数，绝无 exec/eval
```

- 参数分为两类：**列引用**（必须命中 `df.columns` 白名单）与**数值常量**（如周期 `24`，必须可解析为数字）；
- 新特征先在**数据副本**上算出，用于前端出图（变换前后直方图/KDE 对比），全量数据的真实转换同样走该函数，保证预览与导出结果一致。

**（3）代码导出**：把同一条算子记录交给 `export` 模板，拼出带注释、可粘贴进比赛主脚本的 Pandas 代码：

```python
# --- 导出示例（Safe Code Generation，含防错注释） ---
# 1) safe_divide: 做功量 = 压力 / 容积（除零 → NaN，不中断）
df['work'] = np.where(df['volume'] == 0, np.nan,
                      df['pressure'] / df['volume'])
# 2) log1p: 对偏态列取对数（平移至非负，防 log(0) / log(负数)）
df['heat_log'] = np.log1p(df['heat'] - df['heat'].min())
# 3) sin_cos: 小时循环编码
df['hour_sin'] = np.sin(2 * np.pi * df['hour'] / 24)
df['hour_cos'] = np.cos(2 * np.pi * df['hour'] / 24)
```

### 4.4 `app.py` —— Streamlit 交互层

- **Target 配置区**："预测目标（Target）选择"下拉框 = 全部列 + **"无特定目标（无监督模式）"**；选定后联动一个任务类型切换（回归 / 分类），未选目标则自动进入无监督文案；
- **结果展示区**：卡片化展示推荐特征；变换前后直方图 / KDE / 散点图对比；
- **风险警示区**：高危（镜像列）红卡、临界（高缺失）黄卡、安全绿卡；红/黄卡片**默认禁用勾选框**，附"我已了解风险并确认衍生"按钮，点击后解锁；
- **导出区**：多选框勾选（已解锁的）特征，一键复制 / 下载 `.py` 脚本文件。

---

## 5. 关键技术点、暗坑与三道防线

### 防线 A：结构契约 —— 强制 JSON + 受控算子

#### 5.1 严格拦截大模型的"幻觉列名"
> 大模型极易擅自缩写或修改你的列名（例如把 `Current_Ampere` 变成 `current`）。

- **做法**：`codegen.py` 对每条推荐做两层校验：① `op` 必须命中 `SafeOperatorRegistry` 注册表；② `params` 中的列引用必须逐一命中真实 `df.columns`。任一层不通过 → **直接丢弃该建议，不进看板**。（AST/正则解析仅作为可选兜底，用于解析历史遗留的自由表达式。）

#### 5.2 规避纯文本输出，强制结构化 + 算子化
> 不要让大模型直接输出一段 Python 代码或 Markdown 文字，极难解析且不可审计。

- **做法**：API 开启 `response_format={"type": "json_object"}`（或 JSON Schema），规定输出**标准字段、无自由公式**：

```json
{
  "feature_name": "energy_work",
  "op": "safe_divide",
  "params": ["pressure", "volume"],
  "reason": "锅炉做功 ≈ 压力×容积，与能耗强物理相关"
}
```

- 人类可读的"数学表达式"由本地根据 `op` 模板**反查生成**（只用于卡片展示），LLM 不负责、也不允许拼写公式字符串 → 同时消灭了"公式注入"与"公式跑不通"两类问题。

### 防线 B：本地安全执行 —— 受控算子映射，禁用裸 `exec()`

#### 5.3 安全沙箱三铁律（`core/codegen.py`）
1. **严禁 `exec()` / `eval()` 执行大模型返回的任何字符串**——无论它看起来多像合法代码；
2. 一切本地计算只经由 `SafeOperatorRegistry` 白名单映射到**仓库内手写的原生 Python 函数**，行为可审计、可单测；
3. 未注册算子 / 未知参数 → 静默丢弃并记录日志，绝不 fallback 到任意执行。

> 为什么值得这么做：裸 `exec()` 意味着语法错误、运行期异常、甚至恶意注入都会原样进入你的主脚本；而算子化之后，**"生成的代码"本身就不是 LLM 写的，而是本地模板拼的**——语法不可能错、防除零/防负数由模板保证、预览与导出天然一致。这也是"一键生成绝不报错的 Pandas 代码"这条核心价值的技术根基。

### 防线 C：人工确认点（Human-in-the-Loop 安全拦截）

#### 5.4 风险检测规则（`core/profiler.py`）
| 风险类型 | 判定条件 | 级别 | 系统行为 |
| --- | --- | --- | --- |
| **镜像/泄露列** | 两列 $r \ge 0.999$ | 🔴 高危红名单 | 不进 Prompt 候选、本地规则不自动衍生；镜像对只保留其一作为源列 |
| **临界高缺失** | 缺失率 $40\% \sim 70\%$ | 🟡 临界黄名单 | 不静默填充、不作为除法分母或交叉成员；缺失率 $\ge 70\%$ 默认整体剔除（阈值可配置） |
| **常规安全** | 其余情况 | 🟢 正常 | 正常参与推荐与勾选 |

#### 5.5 交互强制确认（`app.py`）
- 高危特征以**红色横幅卡片**、临界特征以**黄色横幅卡片**高亮，并注明判定依据（如"与 `volume` 相关系数 r=0.9998，疑似镜像列"）；
- 红/黄卡片内的特征**默认禁用勾选框**（`disabled=True`），不可直接加入代码导出；
- 用户须手动点击 **"我已了解风险并确认衍生"** → 看板记录本次人工确认 → 解除锁定 → 该特征才可被勾选并进入导出的 `.py`（确认动作是**逐特征、逐会话**的，不做全局放行，防止一次手滑全盘解除）。

#### 5.6 控制单机内存占用（沿用原设计）
> 针对大一、大二级别的常规赛题数据（通常几十万行以内），完全可以保留在内存中。

- **做法**：计算统计量、互信息与生成画像时先对大数据集**下采样**（随机 20,000 行）；得出推荐后对**全量数据**执行转换。个人电脑上始终保持**秒级响应**。

---

## 6. 三大机制与 5 阶段流水线的对应关系（速查）

| 机制 | 融入位置 | 一句话职责 |
| --- | --- | --- |
| **① Target 定义 + 有/无监督自适应** | `app.py`（配置）→ `profiler.py`（分支打分）→ `recommender.py`（Prompt 目标/降级规则） | 有 Label 算"对目标的相关/互信息"；无 Label 自动降级为"方差/PCA/偏度"驱动的无监督推荐 |
| **② 本地安全沙箱 + 受控算子映射** | `codegen.py`（`SafeOperatorRegistry`）贯穿阶段 4 | LLM 只交 `{op, params}`；本地白名单函数执行与出图；禁裸 `exec()`；模板拼接导出原生 Pandas 代码 |
| **③ 人工确认点（HITL）** | `profiler.py`（风险扫描）→ `app.py`（红黄卡 + 默认锁定 + 显式确认） | $r \ge 0.999$ 镜像列与 $40\%\sim70\%$ 高缺失列不得自动放行，确认解锁后才可导出 |

> **设计约束自检**：三条机制全部落在**轻量模块化**框架内——不引入数据库、消息队列或独立沙箱进程；无监督分支复用同一套 5 阶段骨架；沙箱仅是一个注册表 + 若干纯函数；确认点只是前端状态 + 一行禁用逻辑。符合"面向竞赛、轻量、秒级响应"的项目基调。
